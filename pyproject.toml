[project]
name = "agent-network-bus"
version = "0.1.0"
description = "A minimal multi-agent coordination protocol using pub-sub messaging"
requires-python = ">=3.11"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.pytest.ini_options]
testpaths = ["tests"]
